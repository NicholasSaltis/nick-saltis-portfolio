import React from "react";
import { PageContainer } from "../styled-components";

export const Bio = (props) => {


    return(
        <PageContainer>
            <h1>Bio Page</h1>
        </PageContainer>
    )
}